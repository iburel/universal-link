<?xml version="1.0" encoding="utf-8"?>
<!-- The scanner's window (ScanActivity). Framework containers only: the app
     declares no constraintlayout dependency (the template's activity_main.xml is
     never inflated), and a scanner does not need one. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <androidx.camera.view.PreviewView
        android:id="@+id/scan_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:implementationMode="performance"
        app:scaleType="fillCenter" />

    <!-- The instruction and the way out, over the preview rather than beside it:
         the code being framed is somewhere in the middle of the picture, and the
         viewfinder is worth the whole window. Padded from the gesture bar at
         runtime (ScanActivity.insetBar). -->
    <LinearLayout
        android:id="@+id/scan_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#B3000000"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingTop="16dp"
        android:paddingBottom="16dp">

        <TextView
            android:id="@+id/scan_hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:text="@string/scan_hint"
            android:textColor="@android:color/white"
            android:textSize="16sp" />

        <Button
            android:id="@+id/scan_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/scan_cancel" />
    </LinearLayout>
</FrameLayout>
